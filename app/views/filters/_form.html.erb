<%= form_for(@filter) do |f| %>
  <% if @filter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@filter.errors.count, "error") %> prohibited this filter from being saved:</h2>

      <ul>
      <% @filter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :filter_type %><br>
    <%= f.text_field :filter_type %>
  </div>
  <div class="field">
    <%= f.label :regex %><br>
    <%= f.text_field :regex %>
  </div>
  <div class="field">
    <%= f.label :tutorial_id %><br>
    <%= f.select :tutorial_id, options_for_select(Tutorial.all.ids) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
